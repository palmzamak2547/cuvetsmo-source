{
  "name": "cuvetsmo-source-mcp",
  "version": "0.1.0",
  "description": "MCP server exposing the source.cuvetsmo.com verified veterinary drug data plane — search, lookup by ontology code, and citation verification as tools any AI agent (cuvetsmo.com/chat, VetOS, Claude) can call.",
  "type": "module",
  "bin": {
    "cuvetsmo-source-mcp": "./dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node --loader ts-node/esm src/index.ts",
    "smoke": "node dist/smoke.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "typescript": "^5.7.2"
  },
  "engines": { "node": ">=20" },
  "license": "MIT"
}
